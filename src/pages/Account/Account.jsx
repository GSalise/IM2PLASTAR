import React from 'react'
import Header from '../../stuff/Header/Header'

const Account = ({ token }) => {
  return (
    <div>
        <Header token={token} currentpage='account'/>


    </div>
  )
}

export default Account